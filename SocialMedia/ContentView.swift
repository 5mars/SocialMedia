import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {

            Text("I'm testing push/pull git with xcode")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
